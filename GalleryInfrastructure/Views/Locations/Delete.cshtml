@model GalleryDomain.Model.Location

@{
    ViewData["Title"] = "Видалення";
}


<div>
    <h4>Сторінка видалення</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Link)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Link)
        </dd>
    </dl>
    
    <form id="deleteForm" asp-action="Delete" method="post" data-return-url="@Url.Action("Index")">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Видалити локацію" class="btn btn-danger" /> |
        <a asp-action="Index">Всі локації</a>
    </form>
</div>
@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="~/js/deleteConfirmed.js"></script>
}
